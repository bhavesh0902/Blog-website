<%- include("header"); -%>
    <h1>Home</h1>
  <p>  <%= content1 %> </p>
  <% for(let i=0;i<posts.length;i++) { %>
    <h1> <%= posts[i].title %> </h1>
    <p> <%= posts[i].body.substring(0,100) + "..." %>
      <a href="/posts/<%= posts[i]._id %>">Read more...</a>
    </p>
  <% } %>

<%- include("footer"); -%>
